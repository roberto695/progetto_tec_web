services:
  web:
    build: ./web
    ports:
      - "127.0.0.1:8000:80"
    volumes:
      - ./src:/var/www/html
    depends_on:
      - database
  database:
    image: mariadb:latest
    container_name: database
    restart: always
    environment:
      MYSQL_ROOT_PASSWORD: A7l1ks18BATaaUmOqKEktvr4j7xFkaGYlXtM7EQc7rDAWH6nvG
      MYSQL_DATABASE: prelievi_db
      MYSQL_USER: user_web
      MYSQL_PASSWORD: 8A2cU25SoU9zmUrewcib2FgGsY9juEyPrSnFdXBJypa6xfhOmC
    ports:
      - "3307:3306"
    volumes:
      - ./src/db/init_scripts:/docker-entrypoint-initdb.d
      - ./src/db/db_data:/var/lib/mysql  
  phpmyadmin:
    image: phpmyadmin:latest
    restart: always
    ports:
      - "8080:80"
    environment:
      PMA_HOST: database
      PMA_PORT: 3306
    depends_on:
      - database